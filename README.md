# Nbody Simulations

This project compares several implementations of the N body problem.

It includes:
- a basic sequential version
- a Pthreads version for shared memory parallelism
- an MPI version for distributed execution
- a CUDA version for GPU acceleration

The goal is to study performance and scalability for the same simulation model on different computing approaches.
